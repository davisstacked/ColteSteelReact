// Default props

// Components can specify values for missing props.

class Hello extends React.Component {
  static defaultProps = {
    from: 'Anonymous',
    bangs: 1
  };

  render() {
    return ()
  }
}